format_version: 1.1.0
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

app:
  envs:
  - STEP_VERSION: 0.9.3

  # define these in your .bitrise.secrets.yml
  - SAMPLE_REPOSITORY: https://github.com/bitrise-samples/ios-no-shared-schemes.git
  - PROJECT_PATH: BitriseXcode7Sample.xcodeproj
  - CLONE_INTO_DIR: _tmp

workflows:
  test:
    steps:
    - script:
        title: Export GOPATH
        inputs:
        - content: |-
            set -x
            envman add --key GOPATH --value "$BITRISE_SOURCE_DIR/go"
    - script:
        title: "Go Test"
        inputs:
        - working_dir: $BITRISE_SOURCE_DIR/go/src/github.com/bitrise-io/recreate-user-schemes
        - content: |
            set -ex
            go test ./...
    - script:
        title: Step audit
        inputs:
        - content: stepman audit --step-yml=./step.yml
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -e
            set -v
            rm -rf $CLONE_INTO_DIR
        - is_create_path: true
    - change-workdir:
        title: Switch working dir to test / _tmp dir
        inputs:
        - path: $CLONE_INTO_DIR
        - is_create_path: true
    - git-clone:
        run_if: true
        inputs:
        - repository_url: $SAMPLE_REPOSITORY
        - clone_into_dir: ./
        - commit: ""
        - tag: ""
        - branch: "master"
        - pull_request_id: ""
        - clone_depth: ""        
    - path::./:
        title: Step Test
        inputs:
        - project_path: $PROJECT_PATH

  # ----------------------------------------------------------------
  # --- workflows to create Release
  create-release:
    steps:
    - script:
        title:
        inputs:
        - content: |
            #!/bin/bash
            set -e

            export CI=true
            releaseman create --version $STEP_VERSION

  # ----------------------------------------------------------------
  # --- workflows to Share this step into a Step Library
  share-this-step:
    envs:
      # if you want to share this step into a StepLib
      - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL
      - STEP_ID_IN_STEPLIB: recreate-user-schemes
      - STEP_GIT_VERION_TAG_TO_SHARE: $STEP_VERSION
      - STEP_GIT_CLONE_URL: https://github.com/bitrise-steplib/steps-recreate-user-schemes.git
    description: |-
      If this is the first time you try to share a Step you should
      first call: $ bitrise share

      This will print you a guide, and information about how Step sharing
      works. Please read it at least once!

      As noted in the Step sharing guide you'll have to fork the
      StepLib you want to share this step into. Once you're done with forking
      the repository you should set your own fork's git clone URL
      in the `.bitrise.secrets.yml` file, or here in the `envs` section,
      as the value of the `MY_STEPLIB_REPO_FORK_GIT_URL` environment.

      You're now ready to share this Step, just make sure that
      the `STEP_ID_IN_STEPLIB` and `STEP_GIT_VERION_TAG_TO_SHARE`
      environments are set to the desired values!

      To share this Step into a StepLib you can just run: $ bitrise run share-this-step

      Once it finishes the only thing left is to actually create a Pull Request,
      the way described in the guide printed at the end of the process.
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -e
            set -x

            bitrise share start -c ${MY_STEPLIB_REPO_FORK_GIT_URL}

            bitrise share create --stepid ${STEP_ID_IN_STEPLIB} --tag ${STEP_GIT_VERION_TAG_TO_SHARE} --git ${STEP_GIT_CLONE_URL}

            bitrise share finish
